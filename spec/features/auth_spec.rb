require 'spec_helper'

describe "Account Manager Sign Up", :driver => :selenium do
  # before(:all) do
  #   Agency.create({:name => "test"})
  # end

  # let(:email) { Faker::Internet.email }

  # it "signs up an account manager" do
  #   sign_up(email)
  #   page.should have_content(email)
  # end

  # it "signs in an account manager" do
  #   sign_in(email)
  #   page.should have_content(email)
  # end
end
